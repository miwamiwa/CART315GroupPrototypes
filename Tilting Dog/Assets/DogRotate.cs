using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DogRotate : MonoBehaviour
{
    public float rotateValueX = 45.0f;

    private void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //this.transform.Rotate(rotateValueX, 0.0f, 0.0f, Space.Self);
    }
}
